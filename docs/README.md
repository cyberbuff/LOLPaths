# LOLPaths Docs App

This is the Fumadocs/Next.js documentation app for LOLPaths, modeled after the
LOAS docs app.

```bash
bun install
bun run dev
bun run build
```

The MDX source lives in `content/docs`. The homepage reads catalog counts from
`public/api/rules.json`.
